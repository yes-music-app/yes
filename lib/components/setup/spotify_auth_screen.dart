import 'package:flutter/material.dart';

class SpotifyAuthScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("spotify auth"),
    );
  }
}
